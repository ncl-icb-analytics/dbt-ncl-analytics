# Snowflake-supported dbt Core target for this migration
# Keep adapter on the latest 1.10.x patch
dbt-core==1.10.15
dbt-snowflake==1.10.7
pandas==2.3.3
pyyaml==6.0.2
dotenv==0.9.9
keyring==25.6.0
ruamel-yaml>=0.18.0

# snowflake-snowpark-python is intentionally excluded from the base env.
# It currently conflicts with dbt-core 1.10.15 via protobuf constraints.
# Install Snowpark in a separate environment when running Snowpark-specific scripts.

# Note on Dependabot alerts (2026-02-04):
# - deepdiff (CRITICAL): Previously blocked by dbt-common<8.0 pin (addressed by dbt 1.10 upgrade)
# - protobuf (HIGH): Only relevant if using Snowpark tooling in a separate env
# - wheel (HIGH): Transitive dep, pip install --upgrade wheel separately if needed
# These are low practical risk for dbt analytics use cases (no untrusted input processing)
