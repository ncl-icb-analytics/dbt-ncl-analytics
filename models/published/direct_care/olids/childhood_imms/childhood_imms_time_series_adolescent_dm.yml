version: 2

models:
  - name: childhood_imms_time_series_adolescent_dm
    description: Final data model table holding aggregated completion metrics 
      for historical children aged 11 and 16 on analysis month

    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          min_value: 1

