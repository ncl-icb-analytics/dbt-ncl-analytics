models:
  - name: practice_dq_fail_live_dm
    description: Practice list with data quality failures for OLIDS data models. May be empty if no issues.

    config:
      meta:
        owner:
          name: KateHomer55
    # Table may be empty when no practices fail DQ, so no row-count test.
    columns:
      - name: practice_code
        tests:
          - not_null