models:
  - name: adult_imms_person_level_dm
    description: Final data model table holding vaccination detail by dose and demographics for adults aged 65+. Person level data.

    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          arguments:
            min_value: 1
    columns:
      - name: person_id
        tests:
          - not_null