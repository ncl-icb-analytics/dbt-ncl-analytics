models:
  - name: childhood_imms_time_series_child_res_dm_tab
    description: Final data model table holding UNIONED aggregated completion 
      metrics by residential location for historical children aged 1,2 and 5 on 
      analysis month
    config:
      meta:
        owner:
          name: KatsplatH
