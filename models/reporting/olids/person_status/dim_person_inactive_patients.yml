models:
  - name: dim_person_inactive_patients
    description: Dimension table providing inactive patient status at person 
      level. Includes only deceased patients and patients from closed/obsolete 
      practices. Excludes dummy patients.
    config:
      meta:
        owner:
          name: EddieDavison92
    columns:
      - name: person_id
        description: 'Unique person identifier - primary key'
        tests:
          - unique
          - not_null
