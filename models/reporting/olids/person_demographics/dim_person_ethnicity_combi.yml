version: 2

models:
  - name: dim_person_ethnicity_combi
    description: Latest ethnicity per patient combining GP and event-level 
      records. One row per patient with most recent valid ethnicity.
    config:
      meta:
        owner:
          name: Emily Baldwin
    columns:
      - name: sk_patient_id
        description: 'Surrogate key for patient - primary key'
        tests:
          - unique
          - not_null
