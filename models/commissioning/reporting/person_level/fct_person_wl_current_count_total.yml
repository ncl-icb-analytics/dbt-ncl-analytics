version: 1

models:
  - name: int_wl_count_total
    description: >
      Count of open waiting lists per person (patient_id),
      Derived from `int_wl_current`.
    columns:
      - name: patient_id
        description: Unique pseudonymised ID for each patient.