models:
- name: int_sus_apc_spell
  description: 'Admitted Patient Care (APC) spells from SUS data.

    One row per spell.

    '
  columns:
  - name: primarykey_id
    description: Unique identifier for the spell (PRIMARYKEY_ID)
    tests:
    - not_null
    - unique
  tests:
  - dbt_expectations.expect_table_row_count_to_be_between:
      min_value: 1
