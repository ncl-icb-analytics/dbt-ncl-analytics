models:
  - name: int_csds_referrals_summary
    description: |
      Summary of the number of referrals per patient.
      One row per patient (NHS_NUMBER).
    columns:
      - name: sk_patient_id
        description: "Unique pseudo-anonymised patient identifier (NHS_NUMBER)"
        tests:
          - not_null
          - unique