version: 2

models:
  - name: int_csds_encounters
    description: "Event history for community services encounters (contacts) from
      CSDS data"
    config:
      meta:
        owner:
          name: Thomas Shallcross
    columns:
      - name: sk_patient_id
        data_type: varchar
        description: pseudo NHS number
 #       tests:
 #         - not_null -commented out whilst we figure out what to do with null patients IDs
