models:
  - name: int_myria_evaluation
    description: Intermediate table for Myria evaluation (not propensity score matching). Script v18. Aggregated by week prior to intervention -53 to current week by myria_status 
                Discharged, Not onboarded, Onboarded 60+ days, Onboarded less than 60 days

    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          arguments:
            min_value: 1