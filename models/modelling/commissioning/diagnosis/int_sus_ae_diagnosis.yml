
models:
  - name: int_sus_ae_diagnosis
    description: A&E diagnoses from SUS with SNOMED codes mapped to ICD-10. One 
      row per diagnosis per A&E attendance.
    config:
      meta:
        owner:
          name: Emily Baldwin
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - visit_occurrence_id
              - source_concept_code
