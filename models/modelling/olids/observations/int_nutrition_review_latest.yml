models:
  - name: int_nutrition_review_latest
    description: 'Determine latest NUTRITION REVIEW. Built for SMI Longer Lives campaign.'

    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          arguments:
            min_value: 1
    columns:
      - name: person_id
        tests:
          - unique
          - not_null
