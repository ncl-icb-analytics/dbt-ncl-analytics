version: 1
models:
  - name: int_smi_care_plan_all
    description: 'Mental Health /SMI care pan for SMI registry replacement/QOF indicator.
      One row per Mental Health /SMI care pan observation using MHP_COD cluster with currency flag in last 12 months'
    tests:
      - cluster_ids_exist:
          cluster_ids: MHP_COD