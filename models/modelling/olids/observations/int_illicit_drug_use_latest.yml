models:
  - name: int_illicit_drug_use_latest
    description: 'DETERMINE LATEST illicit drug use using PCD Ref ILLSUB_COD'
    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - cluster_ids_exist:
          arguments:
            cluster_ids: ILLSUB_COD