models:
  - name: int_smi_longlives_exercise_assessment
    description: 'Determine exercise patterns for Longer Lives campaign
      using ECL_CACHE Ref SMI_LONGER_LIVES_EXERCISE_ASSESSMENT_COD'

    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          min_value: 1

    columns:
      - name: person_id
        tests:
          - not_null