models:
  - name: int_smi_longlives_dental_inspection_latest
    description: 'Determine latest dental inspection for Longer Lives campaign using EL_CACHE Ref SMI_LONGER_LIVES_DENTAL'

    config:
      meta:
        owner:
          name: KateHomer55
    data_tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          arguments:
            min_value: 1
    columns:
      - name: person_id
        data_tests:
          - unique
          - not_null
