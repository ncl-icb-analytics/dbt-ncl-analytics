models:
  - name: int_cholesterol_ldl_latest
    description: 'Latest LDL (bad) cholesterol measurements for cardiovascular risk
      assessment with clinical categorisation.

      One row per person observation using LDLCCHOL_COD cluster '
    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - cluster_ids_exist:
          cluster_ids: LDLCCHOL_COD
    columns:
      - name: id
        description: Unique identifier for each cholesterol observation
        tests:
          - unique
          - not_null

