version: 2
models:
  - name: int_smi_longlives_subs_misuse_intervention_latest
    description: 'Determine latest substance misuse interventions for Longer Lives
      campaign using PCD REFSET ILLSUBINT_COD'

    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          min_value: 1

    columns:
      - name: person_id
        tests:
          - unique
          - not_null

