models:
  - name: int_csf_leak_all
    description: 'Cerebrospinal fluid leak Observations for PPV Clinical Risk Group using the mapped concept codes: PPV_CLINICAL_RISK_CSF_COD, CSFLATRISK1_COD'
    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - cluster_ids_exist:
          arguments:
            cluster_ids: PPV_CLINICAL_RISK_CSF_COD, CSFLATRISK1_COD