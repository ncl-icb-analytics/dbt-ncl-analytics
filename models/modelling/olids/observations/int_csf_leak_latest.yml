models:
  - name: int_csf_leak_latest
    description: 'Cerebrospinal fluid leak latest Observations for PPV Clinical Risk Group'
    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - cluster_ids_exist:
          arguments:
            cluster_ids: PPV_CLINICAL_RISK_CSF_COD, CSFLATRISK1_COD