models:
  - name: int_smi_longlives_med_review
    description: 'Determine medication review for Longer Lives campaign using EL_CACHE
      Ref SMI_LONGER_LIVES_MEDICINES_REVIEW_COD'

    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          min_value: 1

    columns:
      - name: person_id
        tests:
          - not_null

