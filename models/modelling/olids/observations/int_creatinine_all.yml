models:
  - name: int_creatinine_all
    description: 'Serum creatinine measurements for kidney function assessment and
      CKD monitoring.

      One row per creatinine observation using CRE_COD cluster with plausible range
      filtering (20-1000 µmol/L) and clinical creatinine categorisation.'
    config:
      meta:
        owner:
          name: EddieDavison92
    tests:
      - cluster_ids_exist:
          cluster_ids: CRE_COD
    columns:
      - name: id
        description: Unique identifier for each creatinine observation
        tests:
          - unique
          - not_null
