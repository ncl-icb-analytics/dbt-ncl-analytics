models:
  - name: int_smi_longlives_dental_inspection
    description: 'Determine dental or mouth inspection for Longer Lives campaign using
      EL_CACHE Ref SMI_LONGER_LIVES_DENTAL'

    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          min_value: 1
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - person_id
            - concept_code
            - clinical_effective_date

    columns:
      - name: person_id
        tests:
          - not_null
      - name: concept_code
        tests:
          - not_null
      - name: clinical_effective_date
        tests:
          - not_null
