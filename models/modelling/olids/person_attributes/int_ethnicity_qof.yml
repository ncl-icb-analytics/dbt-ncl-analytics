models:
  - name: int_ethnicity_qof
    description: Intermediate table containing ethnicity information from 
      observations only, with BAME status flags for obesity register.

    config:
      meta:
        owner:
          name: EddieDavison92
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          min_value: 1

    columns:
      - name: person_id
        tests:
          - unique
          - not_null
