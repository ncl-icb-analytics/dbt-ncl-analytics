models:
  - name: int_smi_hc_check_time_series
    description: Intermediate table holding aggregated completied health check metrics by 
      for the SMI population each analysis month. Multiple lines per person
    config:
      meta:
        owner:
          name: KateHomer55
    columns:
      - name: person_id
        description: Unique identifier for the person
        tests:
          - not_null
