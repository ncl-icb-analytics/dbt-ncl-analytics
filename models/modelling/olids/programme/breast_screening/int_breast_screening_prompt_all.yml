version: 1
models:
  - name: int_breast_screening_prompt_all
    description: 'Determine whether a person has received a prompt for breast screening, and if so, the date of the most recent prompt, using EL_CACHE Ref SMI_LONGER_LIVES_CANCER_SCREEN_PROMPTS cluster'
    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          min_value: 1

    columns:
      - name: person_id
        tests:
          - not_null