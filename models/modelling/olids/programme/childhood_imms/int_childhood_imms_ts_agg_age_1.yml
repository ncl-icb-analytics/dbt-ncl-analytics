models:
  - name: int_childhood_imms_ts_agg_age_1
    description: Intermediate table holding aggregated completion metrics for 
      historical children aged 1 at each analysis month.
    config:
      meta:
        owner:
          name: KatsplatH
