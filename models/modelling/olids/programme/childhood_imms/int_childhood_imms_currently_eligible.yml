models:
  - name: int_childhood_imms_currently_eligible
    description: Intermediate table to cross join currently childhood imms 
      population with Imms Schedule to get eligibility window. Multiple lines 
      per person
    config:
      meta:
        owner:
          name: KateHomer55
    columns:
      - name: person_id
        description: Unique identifier for the person
        tests:
          - not_null

