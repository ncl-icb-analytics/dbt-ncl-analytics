models:
  - name: int_childhood_imms_overview_child
    description: Intermediate table holding aggregated completion metrics for 
      currently registered children aged 1,2 and 5 on current date.
    config:
      meta:
        owner:
          name: Kate Homer
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          min_value: 1
