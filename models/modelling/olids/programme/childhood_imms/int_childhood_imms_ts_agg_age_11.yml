models:
  - name: int_childhood_imms_ts_agg_age_11
    description: Intermediate table holding aggregated completion metrics for 
      historical children aged 11 at each analysis month.
    config:
      meta:
        owner:
          name: KatsplatH
