models:
  - name: int_childhood_imms_ts_agg_age_2
    description: Intermediate table holding aggregated completion metrics for 
      historical children aged 2 at each analysis month.
    config:
      meta:
        owner:
          name: Kate Homer
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          min_value: 1
