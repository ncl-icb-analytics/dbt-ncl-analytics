models:
  - name: int_childhood_imms_ts_agg_res_age_1
    description: Intermediate table holding aggregated completion metrics by 
      residential location for historical children aged 1 at each analysis 
      month.
    config:
      meta:
        owner:
          name: Kate Homer
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          min_value: 1
