models:
  - name: int_childhood_imms_eoy_count_agg_age_5
    description: Intermediate table holding aggregated dose counts per month for
      historical children aged 5 at the end of each fiscal year
    config:
      meta:
        owner:
          name: KatsplatH
