models:
  - name: int_childhood_imms_eoy_count_agg_age_1
    description: Intermediate table holding aggregated dose counts per month for
      historical children aged 1 at the end of each fiscal year
    config:
      meta:
        owner:
          name: KatsplatH
