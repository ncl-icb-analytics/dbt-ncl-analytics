models:
  - name: int_childhood_imms_vaccs_current_age_2
    description: Intermediate table for eligble population aged 2 years old by current time_stamp and who have complete or outofschedule vaccinations (declined, contraindicated and overdue excluded). Person level data.

    columns:
      - name: person_id
        description: Unique identifier for the person
        tests:
          - not_null
          - unique