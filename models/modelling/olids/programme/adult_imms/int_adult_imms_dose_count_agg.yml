models:
  - name: int_adult_imms_dose_count_agg
    description: Intermediate table for aggregate counts of vaccinations by month for historical population aged 60+ years old.
    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          arguments:
            min_value: 1