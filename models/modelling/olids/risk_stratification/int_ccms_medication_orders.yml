models:
  - name: int_ccms_medication_orders
    description: |
      Medication orders restricted to the CCMS dm+d codelist and pre-filtered
      to the Cambridge Comorbidity Score windows (last 12 months for all
      conditions; any prescription ever for lithium / conditionid 5029).
      View materialization.
    config:
      meta:
        owner:
          name: EddieDavison92
    columns:
      - name: person_id
        description: Person identifier.
        data_tests:
          - not_null
      - name: medication_order_id
        description: stg_olids_medication_order.id.
      - name: order_date
        description: Clinical effective date of the order.
        data_tests:
          - not_null
      - name: mapped_concept_code
        description: dm+d code on the order, joined to BASE_CCMS_DMD_CODES.productid.
      - name: conditionid
        description: CCMS condition identifier from BASE_CCMS_DMD_CODES.
        data_tests:
          - not_null
      - name: conditionname
        description: CCMS condition name from BASE_CCMS_DMD_CODES.
