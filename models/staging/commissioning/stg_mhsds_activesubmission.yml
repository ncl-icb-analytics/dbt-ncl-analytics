models:
  - name: stg_mhsds_activesubmission
    description: Table containing active submissions in MHSDS data
    config:
      meta:
        owner:
          name: Jessica Roberts
    columns:
      - name: uniq_submission_id
        description: MHSDS unique submission identifier
        tests:
          - unique
          - not_null
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          min_value: 1
