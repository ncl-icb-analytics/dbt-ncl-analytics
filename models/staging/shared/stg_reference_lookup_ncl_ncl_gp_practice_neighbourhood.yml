models:
  - name: stg_reference_lookup_ncl_ncl_gp_practice_neighbourhood
    description: Mapping between GP practice codes and NCL neighbourhood codes 
      from reference lookup
    config:
      meta:
        owner:
          name: Jake Kealey
    columns:
      - name: practice_code
        description: Primary key - GP practice code
        tests:
          - unique
          - not_null
      - name: practice_name
        description: GP practice name
      - name: neighbourhood_code
        description: NCL neighbourhood code
      - name: neighbourhood_name
        description: NCL neighbourhood name
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          min_value: 1
