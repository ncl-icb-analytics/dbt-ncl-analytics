models:
  - name: stg_reference_ncl_interpreter_required
    description: Interpreter required flag lookup
    config:
      meta:
        owner:
          name: EddieDavison92
    columns:
      - name: interpreter_required
        description: Primary key - interpreter required flag value
        data_tests:
          - unique
          - not_null
