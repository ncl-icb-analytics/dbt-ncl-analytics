models:
  - name: stg_dictionary_op_attendancetypes
    description: Outpatient attendance type codes reference
    config:
      meta:
        owner:
          name: Emily Baldwin
    columns:
      - name: sk_attendance_type
        description: Primary key - surrogate key for attendance type
        tests:
          - unique
          - not_null
      - name: bk_attendance_type_code
        description: Business key - attendance type code
        tests:
          - not_null
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          min_value: 1
