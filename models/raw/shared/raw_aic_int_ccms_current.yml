version: 2

models:
  - name: raw_aic_int_ccms_current
    description: Raw layer (AIC pipelines) passthrough for current Cambridge comorbidity score records.
    columns:
      - name: person_id
        description: Person identifier from the upstream AIC source table.
        tests:
          - not_null
          - unique
