version: 2

models:
  - name: raw_registries_births_births
    description: "ONS births register data"
    config:
      meta:
        owner:
          name: PeterSNHS
    columns:
      - name: dmic_row_id
        description: "Unique row identifier"
        data_tests:
          - unique
          - not_null

  - name: raw_registries_deaths_deaths
    description: "ONS deaths register data"
    config:
      meta:
        owner:
          name: PeterSNHS
    columns:
      - name: dmic_row_id
        description: "Unique row identifier"
        data_tests:
          - unique
          - not_null
